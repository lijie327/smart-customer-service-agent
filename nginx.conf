# 反向代理：将 80 端口流量转发给后端 app（app 同时托管前端静态资源）
# SSE 长连接需要关闭代理缓冲并放大读取超时。
server {
    listen 80;
    server_name _;

    # 上传 FAQ 文件的最大体积
    client_max_body_size 20m;

    location / {
        proxy_pass http://app:8000;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        # SSE / 长连接支持
        proxy_set_header Connection '';
        proxy_read_timeout 3600s;
        proxy_send_timeout 3600s;
        proxy_buffering off;
        chunked_transfer_encoding on;
    }
}

# ===== HTTPS 参考（可选，配合阿里云 SSL 证书）=====
# server {
#     listen 443 ssl;
#     server_name your-domain.com;
#     ssl_certificate     /etc/nginx/certs/your-domain.com.pem;
#     ssl_certificate_key /etc/nginx/certs/your-domain.com.key;
#     ssl_protocols TLSv1.2 TLSv1.3;
#     location / {
#         proxy_pass http://app:8000;
#         proxy_http_version 1.1;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#         proxy_set_header Connection '';
#         proxy_read_timeout 3600s;
#         proxy_buffering off;
#     }
# }
# server {
#     listen 80;
#     server_name your-domain.com;
#     return 301 https://$host$request_uri;   # HTTP 强制跳转 HTTPS
# }
